<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="ir_cron_check_expired_subscriptions" model="ir.cron">
        <field name="name">Check Expired Wallbox Subscriptions</field>
        <field name="model_id" ref="model_wallbox_subscription"/>
        <field name="state">code</field>
        <field name="code">model._cron_check_expired_subscriptions()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="active">True</field>
    </record>

</odoo>
